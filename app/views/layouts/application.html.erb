<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body class="text-white bg-dark">

    <nav class="nav justify-content-end mt-1 mr-2"> 
      <%= link_to 'myBlog', root_path, class: "btn btn-link" %>
      <% if user_signed_in? %>

        <form class="form-inline mr-2">
          <%= link_to 'Salir', destroy_user_session_path, method: :delete, class: "btn btn-info" %>
        </form>
         
        <% else %>

        <form class="form-inline mr-2">
          <%= link_to 'Iniciar sesión', new_user_session_path, class: "btn btn-info" %>
        </form>

      <% end %>
      
    </nav>
    
    <div class="container">
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>
      <%= yield %>
    </div>
   
  </body>
</html>
